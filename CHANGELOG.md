# Changelog

本项目的所有重要变更都会记录在此文件。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.0] - 2026-09-21

首个可用版本。

### 新增

- **LaTeX 代码补全**：在公式编辑窗口中输入英文字母即弹出补全（无需先输入 `\`）。
  - 命令库约 1000 条，由 KaTeX 源码（`symbols.ts` / `functions/*.ts` /
    `environments/*.ts` / `macros.ts`）自动生成，另叠加人工精选片段；
  - 前缀 / 子串 / 模糊三级匹配，`↑` `↓` 选择、`Enter` 确认、`Esc` 关闭，支持鼠标点击；
  - 候选项用 KaTeX 实时渲染，并高亮命中的字母；
  - 确认后自动补齐花括号 / 方括号，光标落入第一个括号；
    `Tab` 依次跳到下一个括号，最后一个括号后再按 `Tab` 直接跳出片段，
    `Shift+Tab` 可往回跳；
  - 插入使用 `execCommand("insertText")`，保留输入框的原生撤销（`Ctrl+Z`）。
- 完整的设置面板（12 项）与中英文 i18n。
- 工程化：TypeScript + esbuild 构建、三套自动化测试、GitHub Actions CI 与发布流程。

### 修复

- **方向键呼出公式编辑窗口时默认全选**：改为折叠光标，不再选中文本；
  光标按进入方向落在公式开头或末尾（也可固定为开头 / 末尾）。
  根因是思源核心在 `keydown` 中先 `range.selectNode(公式节点)`，
  打开面板时又执行了一次 `textarea.select()`。
- **`Tab` 被补全抢占**：在花括号内输入字母会弹出补全，导致 `Tab` 被用于确认补全
  而不是跳到下一个括号；现在片段还有未填完的括号时 `Tab` 优先用于跳转。
- **补全弹窗遮挡编辑框**：原先用「未截断的自然高度」决定上下翻转，导致落点算错并压住编辑框；
  现在先按可用空间限制高度、再测量、再定位，弹窗只在编辑框正上方或正下方出现，零重叠。
- **补全列表文字溢出菜单**：弹窗改为自带底色 / 边框 / 阴影并强制裁剪，
  不再依赖主题是否定义 `--b3-menu-background`（该变量只在主题 CSS 中定义，
  `base.css` 里没有，缺失时弹窗会变成透明）。

### 测试

- 24 项纯逻辑测试（命令库检索、片段解析）；
- 36 项无头 Chromium 端到端测试（用真实构建产物、按思源相同方式加载）；
- 27 项布局回归测试（加载思源真实 `base.css` 与主题，含浏览器命中测试探测）。

[1.0.0]: https://github.com/PrintfCow/siyuan-katex-helper/releases/tag/v1.0.0
